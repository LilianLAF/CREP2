# resultats

Résultats de comparaison inter-modèles.

Les fichiers de sortie de chaque modèle (PNG, CSV) sont générés dans le dossier `resultats/` propre à chaque modèle (`Modèle X/resultats/`). Ce dossier racine est réservé aux graphiques comparant plusieurs modèles entre eux.
