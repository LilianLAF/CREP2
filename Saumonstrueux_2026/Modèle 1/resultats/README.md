

# Modèle 1 — resultats

Sorties générées par `MAIN.py` du Modèle 1.

Chaque exécution produit :
- `temperature_{lat}N_{lon}E_{annee}.png` : courbe de température de surface sur l'année.
- `temperature_{lat}N_{lon}E_{annee}.csv` : série temporelle horaire (Date, Température en K).
